# GoReleaser configuration for dws
# Docs: https://goreleaser.com
#
# To release, use scripts/release/release.sh. It seals main, validates the
# CHANGELOG and packages, then pushes the annotated tag for CI/CD to publish.
#
# CGO release builds must run through scripts/release/run-goreleaser-cross.sh,
# which supplies the pinned Darwin/Linux/Windows cross toolchains.
#
# To test locally (no publish):
#   make package VERSION=v0.0.0-SNAPSHOT

version: 2

builds:
  - main: ./cmd
    binary: dws
    env:
      - CGO_ENABLED=1
      - GOTOOLCHAIN=go1.25.9
      - CC_darwin_amd64=o64-clang
      - CXX_darwin_amd64=o64-clang++
      - CC_darwin_arm64=oa64-clang
      - CXX_darwin_arm64=oa64-clang++
      - CC_linux_amd64=/opt/dws-zig/zig cc -target x86_64-linux-gnu.2.17
      - CXX_linux_amd64=/opt/dws-zig/zig c++ -target x86_64-linux-gnu.2.17
      - CC_linux_arm64=/opt/dws-zig/zig cc -target aarch64-linux-gnu.2.17
      - CXX_linux_arm64=/opt/dws-zig/zig c++ -target aarch64-linux-gnu.2.17
      - CC_windows_amd64=x86_64-w64-mingw32-gcc
      - CXX_windows_amd64=x86_64-w64-mingw32-g++
      - CC_windows_arm64=/llvm-mingw/bin/aarch64-w64-mingw32-gcc
      - CXX_windows_arm64=/llvm-mingw/bin/aarch64-w64-mingw32-g++
      - 'CC={{ index .Env (print "CC_" .Os "_" .Arch) }}'
      - 'CXX={{ index .Env (print "CXX_" .Os "_" .Arch) }}'
    flags:
      - -buildmode=pie
      - -trimpath
    ldflags:
      - -s -w
      - -X github.com/DingTalk-Real-AI/dingtalk-workspace-cli/internal/app.version=v{{.Version}}
      - -X github.com/DingTalk-Real-AI/dingtalk-workspace-cli/internal/app.gitCommit={{.Commit}}
      - -X github.com/DingTalk-Real-AI/dingtalk-workspace-cli/internal/app.buildTime={{.CommitDate}}
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - formats:
      - tar.gz
    name_template: "dws-{{ .Os }}-{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats:
          - zip
    files:
      - LICENSE
      - NOTICE
      - README.md
      - CHANGELOG.md

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

snapshot:
  version_template: "{{ .Env.DWS_PACKAGE_VERSION }}"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - "^chore:"

release:
  github:
    # 用当前运行 CI 的仓库 owner: fork CI 发到 fork, 官方 CI 发到官方, 两边都对
    owner: "{{ .Env.GITHUB_REPOSITORY_OWNER }}"
    name: dingtalk-workspace-cli
  # Keep the release private until post-processing has replaced the Darwin
  # archives and verified every finalized asset digest.
  draft: true
  prerelease: auto
  name_template: "v{{.Version}}"
  mode: replace
